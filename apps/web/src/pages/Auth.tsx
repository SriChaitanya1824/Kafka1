import { FormEvent, useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import { login, register } from "../services/api";
export function Auth({ mode }: { mode: "login" | "register" }) { const nav=useNavigate(); const [form,setForm]=useState({name:"",email:"",password:""}); const submit=async(e:FormEvent)=>{e.preventDefault(); mode==="login"?await login(form.email,form.password):await register(form); nav("/dashboard")}; return <section className="auth"><form onSubmit={submit}><h1>{mode==="login"?"Login":"Register"}</h1>{mode==="register"&&<input data-testid="name" placeholder="Name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/>}<input data-testid="email" placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input data-testid="password" placeholder="Password" type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><button data-testid="submit">{mode==="login"?"Login":"Create account"}</button><Link to={mode==="login"?"/register":"/login"}>{mode==="login"?"Need an account?":"Have an account?"}</Link></form></section> }
