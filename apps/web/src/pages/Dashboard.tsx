import { useEffect, useState } from "react"; import { api } from "../services/api";
export function Dashboard(){const [data,setData]=useState<any>({});useEffect(()=>{api.get("/analytics/overview").then(r=>setData(r.data.data)).catch(()=>undefined)},[]);return <section><h1>Dashboard</h1><div className="cards">{["total","delivered","failed","deliveryRate"].map(k=><article className="card" key={k}><span>{k}</span><strong>{k.includes("Rate")?`${Math.round((data[k]??0)*100)}%`:data[k]??0}</strong></article>)}</div><div className="panel"><h2>Recent Notifications</h2><NotificationTable/></div></section>}
export function NotificationTable(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{api.get("/notifications").then(r=>setRows(r.data.data.data??[])).catch(()=>undefined)},[]);return <table><tbody>{rows.map(n=><tr key={n._id}><td>{n.channel}</td><td>{n.status}</td><td>{new Date(n.createdAt).toLocaleString()}</td></tr>)}</tbody></table>}
