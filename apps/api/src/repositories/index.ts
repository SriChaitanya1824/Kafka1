import { BaseRepository } from "./base.js";
import { User } from "../models/User.js";
import { Template } from "../models/Template.js";
import { Workflow } from "../models/Workflow.js";
import { Notification } from "../models/Notification.js";
import { DeliveryAttempt } from "../models/DeliveryAttempt.js";
import { Preference } from "../models/Preference.js";
import { InAppNotification } from "../models/InAppNotification.js";
import { ProviderConfig } from "../models/ProviderConfig.js";
import { IdempotencyKey } from "../models/IdempotencyKey.js";
export const users = new BaseRepository(User);
export const templates = new BaseRepository(Template);
export const workflows = new BaseRepository(Workflow);
export const notifications = new BaseRepository(Notification);
export const attempts = new BaseRepository(DeliveryAttempt);
export const preferences = new BaseRepository(Preference);
export const inAppNotifications = new BaseRepository(InAppNotification);
export const providerConfigs = new BaseRepository(ProviderConfig);
export const idempotencyKeys = new BaseRepository(IdempotencyKey);
